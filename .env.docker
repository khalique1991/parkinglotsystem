# Docker environment variables - KEEP SECRETS OUT OF VERSION CONTROL
# For development use only. Use HashiCorp Vault in production.

# Database Configuration
DB_NAME=parkingdb
DB_USER=postgres
DB_PASSWORD=root_change_in_production
DB_USER_NAME=userdb
DB_USER=user
DB_USER_PASSWORD=userpass_change_in_production
DB_SESSION_NAME=sessiondb
DB_SESSION_USER=session
DB_SESSION_PASSWORD=sessionpass_change_in_production

# Vault Configuration
VAULT_ADDR=http://vault:8200
VAULT_TOKEN=root_change_in_production

# JWT Secret (should be 256-bit for HS256)
JWT_SECRET=cGhzSmpSNXhRVXMyTTh2MVIweTl6S3FMNmFZd0I3blRnVnhjRjhkaEpxUjlzVmJXbU4wZVh5WjFyUTJ0VQ==
JWT_EXPIRATION_MS=3600000

# Mail Configuration
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password

# Spring Profile
SPRING_PROFILE=docker

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:8080

# Frontend Configuration
VITE_API_BASE_URL=http://localhost:8080/api

# Optional: Add for production deployments with Kubernetes
# NAMESPACE=parking-system-prod
# ENVIRONMENT=production
