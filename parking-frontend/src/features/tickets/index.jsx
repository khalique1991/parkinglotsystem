import TicketList from './TicketsList';
import CreateTicketForm from './CreateTicketForm';
import TicketDetail from './TicketDetail';

// Named exports
export { TicketList, CreateTicketForm, TicketDetail };

// Optional default export (not necessary if you use named imports)
export default {
  TicketList,
  CreateTicketForm,
  TicketDetail
};
